# 快速入门指南 - GaussianSplats3D

## 如何运行代码

本指南将帮助您在本地环境中运行 GaussianSplats3D 项目。

---

## 📋 前置要求

在开始之前，请确保您的系统已安装：

1. **Node.js** (版本 14 或更高)
   - 下载地址: https://nodejs.org/
   - 验证安装: 在终端运行 `node --version`

2. **npm** (通常随 Node.js 一起安装)
   - 验证安装: 在终端运行 `npm --version`

---

## 🚀 快速开始

### 步骤 1: 安装依赖

首先，在项目目录中打开终端，运行以下命令安装所需的依赖包：

```bash
npm install
```

这将下载并安装所有必要的 Node.js 包，包括 Three.js 和其他构建工具。

⏱️ **预计时间**: 1-3 分钟（取决于您的网络速度）

---

### 步骤 2: 构建项目

安装完依赖后，需要构建项目。根据您的操作系统选择相应的命令：

#### Linux 和 macOS 用户:
```bash
npm run build
```

#### Windows 用户:
```bash
npm run build-windows
```

这个命令将：
- 编译源代码
- 生成优化后的 JavaScript 文件
- 创建 `build/` 目录和 `build/demo/lib/` 文件夹
- 准备演示文件

📖 **想了解 lib 文件夹是如何创建的？** 查看 [lib文件夹创建说明.md](./lib文件夹创建说明.md)

⏱️ **预计时间**: 10-30 秒

---

### 步骤 3: 下载演示数据（可选）

如果您想查看完整的演示场景，需要下载示例数据：

1. 下载数据文件: [gaussian_splat_data.zip](https://projects.markkellogg.org/downloads/gaussian_splat_data.zip)
2. 解压文件
3. 将解压后的文件放入以下目录:
   ```
   build/demo/assets/data/
   ```

📁 **目录结构示例**:
```
build/
└── demo/
    └── assets/
        └── data/
            ├── bonsai/
            ├── garden/
            ├── stump/
            └── truck/
```

---

### 步骤 4: 启动本地服务器

现在可以启动本地开发服务器了：

```bash
npm run demo
```

您应该看到类似这样的输出：
```
Server running at 0.0.0.0:8080
```

---

### 步骤 5: 在浏览器中查看

打开您的网络浏览器，访问:

🌐 **http://localhost:8080/index.html**

或者

🌐 **http://127.0.0.1:8080/index.html**

---

## 📂 项目结构

```
GaussianSplats3D/
├── src/                    # 源代码
├── demo/                   # 演示 HTML 文件
├── build/                  # 构建输出（运行 build 后生成）
│   └── demo/
│       ├── lib/            # JavaScript 库
│       └── assets/         # 资源文件
├── package.json            # 项目配置
└── README.md              # 英文文档
```

---

## 🎮 控制说明

### 鼠标控制
- **左键点击**: 设置焦点
- **左键拖动**: 围绕焦点旋转
- **右键拖动**: 平移相机和焦点

### 键盘快捷键
- `C`: 切换网格光标
- `I`: 切换信息面板（显示 FPS、相机位置等）
- `U`: 切换调试对象
- `←`: 逆时针旋转相机
- `→`: 顺时针旋转相机
- `P`: 切换点云模式
- `=`: 增加点云缩放
- `-`: 减少点云缩放
- `O`: 切换正交模式

---

## 🔧 常见问题

### 问题 1: 运行 `npm install` 时出错
**解决方案**: 
- 确保 Node.js 版本 >= 14
- 尝试清除缓存: `npm cache clean --force`
- 删除 `node_modules` 文件夹和 `package-lock.json`，然后重新运行 `npm install`

### 问题 2: 构建失败
**解决方案**:
- 确保已正确运行 `npm install`
- 检查是否有足够的磁盘空间
- 在 Windows 上确保使用 `npm run build-windows`

### 问题 3: 页面显示但看不到 3D 场景
**解决方案**:
- 确保已下载并正确放置演示数据文件
- 检查浏览器控制台是否有错误信息
- 确保浏览器支持 WebGL 2.0

### 问题 4: 端口 8080 已被占用
**解决方案**:
- 找到并关闭占用端口的程序
- 或者修改 `util/server.js` 中的端口号

---

## 📚 更多信息

### 支持的文件格式
- `.ply` - 原始 PLY 文件
- `.splat` - 标准 splat 文件
- `.ksplat` - 压缩的自定义格式（推荐）

### 在代码中使用

如果您想在自己的项目中使用这个库：

```javascript
import * as GaussianSplats3D from '@mkkellogg/gaussian-splats-3d';

const viewer = new GaussianSplats3D.Viewer({
    'cameraUp': [0, -1, -0.6],
    'initialCameraPosition': [-1, -4, 6],
    'initialCameraLookAt': [0, 4, 0]
});

viewer.addSplatScene('<文件路径.ply>', {
    'splatAlphaRemovalThreshold': 5,
    'showLoadingUI': true,
    'position': [0, 1, 0],
    'rotation': [0, 0, 0, 1],
    'scale': [1.0, 1.0, 1.0]
});
```

### 阴影映射功能

本项目支持 WebGL2 阴影映射（需要 WebGL2）：

```javascript
const viewer = new GaussianSplats3D.Viewer({
    'enableShadowsOnSplats': true,      // 启用阴影
    'enablePCSSOnSplats': false,         // 使用 PCSS（软阴影）
    'shadowMapResolution': 1024,         // 阴影贴图分辨率
    'shadowBias': 0.001,                 // 阴影偏移
    'shadowLightDirection': [0.5, -1.0, 0.5],  // 光源方向
    'threeScene': myThreeScene           // Three.js 场景
});
```

更多详情请查看 `SHADOW_MAPPING.md`

---

## 🔗 相关链接

- [英文 README](./README.md)
- [在线演示](https://projects.markkellogg.org/threejs/demo_gaussian_splats_3d.php)
- [阴影映射文档](./SHADOW_MAPPING.md)
- [原项目 GitHub](https://github.com/mkkellogg/GaussianSplats3D)

---

## 💡 提示

1. **性能优化**: 将 `.ply` 或 `.splat` 文件转换为 `.ksplat` 格式可获得最佳性能
2. **大型场景**: 如遇到大型场景的问题，尝试设置 `integerBasedSort: false`
3. **开发模式**: 使用 `npm run watch` 可在修改代码时自动重新构建

---

## 📝 下一步

成功运行后，您可以：

1. 尝试加载自己的 `.ply` 或 `.splat` 文件
2. 修改演示页面代码（在 `demo/` 目录中）
3. 探索源代码（在 `src/` 目录中）
4. 参考 [README.md](./README.md) 了解更多 API 选项

---

**祝您使用愉快！** 🎉

如有问题，请查看 [GitHub Issues](https://github.com/mkkellogg/GaussianSplats3D/issues)
